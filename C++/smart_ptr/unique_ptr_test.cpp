#include <bits/stdc++.h>
using namespace std;
int main(){
  unique_ptr<int>up(new int(3));
  //all wrong since it does not support the two oeprations
//   unique_ptr<int>p(up);
//   unique_ptr<int>k=up;
  return 0;
}